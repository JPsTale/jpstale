<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jpstale.dao.gamedb.mapper.NpcListMapper">

    <!-- C++ unitinfo.cpp:1487 -->
    <select id="selectById" resultType="org.jpstale.dao.gamedb.entity.NpcList">
        SELECT * FROM gamedb.npc_list WHERE id = #{id}
    </select>
    <!-- servercommand.cpp:3940 -->
    <select id="selectCountById" resultType="java.lang.Integer">
        SELECT COUNT(id) FROM gamedb.npc_list WHERE id = #{id}
    </select>

</mapper>
